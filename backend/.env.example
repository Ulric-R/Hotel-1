# Example DATABASE_URL for MySQL
# Replace user, password, host, port and database name accordingly
# DATABASE_URL=mysql+pymysql://user:password@127.0.0.1:3306/Fanja

# If you don't have MySQL installed locally, the app will use a local
# SQLite file by default: sqlite:///./data/Fanja.db
